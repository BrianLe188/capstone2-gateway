import Joi from "joi";

export const applicationAdmissionRegistrationSchema = Joi.object({});
export const applicationForAdmissionConsiderationAccordingToTheCompetenceAssessmentTestResult =
  Joi.object({});
export const applicationForAdmissionWithAHighSchoolScript = Joi.object({});
export const applicationForStraightAdmissionAndPriorityConsideration =
  Joi.object({});
