import path from "path";

export const ADMISSION_PROTO_PATH = path.join(
  __dirname,
  "admission",
  "admission.proto"
);

export const AUTH_PROTO_PATH = path.join(__dirname, "auth", "auth.proto");
